{
  "name": "colors-helper-tools-monorepo",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "cht": "pnpm --filter colors-helper-tools",
    "clean": "rm -rf node_modules && pnpm -r exec rm -rf node_modules dist && pnpm store prune"
  },
  "devDependencies": {
    "@types/node": "^20.19.13",
    "@types/prompts": "^2.4.9",
    "@types/readline-sync": "^1.4.8",
    "@types/yargs": "^17.0.33"
  }
}